#pragma once

#include "sch.h"

/*
class Manager;

class Task
{
private:
	int _tsk_status;
	int _tsk_id;

	std::function<void()> _tsk_fnc;

	Manager* _tsk_parent;

public:
	Task();
	Task(std::function<void()> _t_fn);
	Task(std::function<void()> _t_fn, int _id);

	/* Run the function attached to the task
	*      Note : if a function isnot attached, the run function will throw an exception
	*//*
	bool runTask();
	void deleteTask();

	void setparent(Manager* _p)
	{
		_tsk_parent = _p;
	}

	int getID()
	{
		return _tsk_id;
	}

	void waitForTask();
};

*/

